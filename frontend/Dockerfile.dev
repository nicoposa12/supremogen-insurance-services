FROM node:20-alpine

WORKDIR /app

# Copy package config first to cache dependency layers
COPY package*.json ./
RUN npm install

# Copy the rest of the application files
COPY . .

EXPOSE 5173

# Start Vite dev server exposing to host network
CMD ["npm", "run", "dev", "--", "--host"]
